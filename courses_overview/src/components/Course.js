import React from 'react';

const course = (props) => {
    <div>
        <label>Id : </label>{props.id}
    </div>
    <div>
        <label>Name : </label>{props.name}
    </div>
};

export default course;